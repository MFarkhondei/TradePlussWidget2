plugins {
    id("com.android.application")
}

android {
    namespace "ir.tradeplus.widget"
    compileSdk 34

    defaultConfig {
        applicationId "ir.tradeplus.widget"
        minSdk 24
        targetSdk 34
        versionCode 2
        versionName "1.0.1"
    }

    buildTypes {
        release {
            minifyEnabled false
        }
        debug {
            minifyEnabled false
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}
